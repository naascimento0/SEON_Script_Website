# SEON Application Environment Variables

# Admin user credentials
SEON_ADMIN_USERNAME=admin
SEON_ADMIN_PASSWORD=CHANGE_ME

# Where uploaded .asta files are archived (version history shown to admins on /upload).
# Relative to the working directory; created on first upload.
SEON_ASTA_VERSIONS_DIR=asta-versions
